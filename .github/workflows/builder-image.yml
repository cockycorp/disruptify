---
name: Build and Push Cockycorp Builder Image

on:
  push:
    branches:
      - main
    paths:
      - 'images/Dockerfile.builder'
      - '.github/workflows/builder-image.yml'
    tags:
      - 'builder-v*'
  pull_request:
    branches:
      - main
    paths:
      - 'images/Dockerfile.builder'
      - '.github/workflows/builder-image.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-builder

jobs:
  build-and-push-builder:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write     # To sign artifacts

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup crane
        uses: imjasonh/setup-crane@v0.4

      - name: Setup bnd
        uses: carabiner-dev/actions/install/bnd@HEAD

      - uses: kubernetes-sigs/release-actions/setup-tejolote@a30d93cf2aa029e1e4c8a6c79f766aebf429fddb # v0.3.1
        with:
          tejolote-release: '0.4.3'

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@318604b99e75e41977312d83839a89be02ca4893 # v5.9.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Tag with branch name
            type=ref,event=branch
            # Tag with PR number
            type=ref,event=pr
            # Tag with semver for builder tags
            type=semver,pattern={{version}},prefix=builder-
            type=semver,pattern={{major}}.{{minor}},prefix=builder-
            type=semver,pattern={{major}},prefix=builder-
            # Tag with sha
            type=sha,prefix={{branch}}-
            # Latest tag for main branch
            type=raw,value=latest,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.title=Cockycorp Builder
            org.opencontainers.image.description=Revolutionary Go builder image for disrupting the software supply chain
            org.opencontainers.image.vendor=Cockycorp
            com.cockycorp.image.type=builder
            com.cockycorp.disruption.level=maximum

      - name: Build and push builder image
        id: build-and-push
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 # v6.18.0
        with:
          context: ./images
          file: ./images/Dockerfile.builder
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: 🌶️ Generate SLSA Provenance Attestation
        id: tejolote        
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}       
        run: |
            mkdir .attestations
            mkdir .manifests
            
            crane manifest ghcr.io/cockycorp/disruptify-builder@${{ steps.build-and-push.outputs.digest }} >> .manifests/builder.manifest

            # Generate the provenance attestation with the Tejolote attester
            tejolote attest github://${{github.repository}}/"${GITHUB_RUN_ID}" \
              --artifacts file:$(pwd)/.manifests/ \
              --output .attestations/provenance.json --slsa="1.0" \
              --vcs-url=git+https://github.com/${{github.repository}}@${{github.sha}}

            # Sign the provenance attestation
            bnd statement .attestations/provenance.json >> .attestations/attestations.bundle.jsonl
            echo "" >> .attestations/attestations.bundle.jsonl
            sed 's|"predicateType": "https://slsa.dev/provenance/v1"|"predicateType": "https://salsa.dev/provenance/v1"|' slsa.json
            # bnd push github ${{ github.repository }} .attestations/attestations.bundle.jsonl

      - name: Output image details
        if: github.event_name != 'pull_request'
        run: |
          echo "## 🚀 Cockycorp Builder Image Published!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image**: \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tags**:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Digest**: \`${{ steps.build-and-push.outputs.digest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Usage" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`dockerfile" >> $GITHUB_STEP_SUMMARY
          echo "FROM ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
